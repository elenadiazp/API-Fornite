class noticia {
    constructor({ id, title, tabTitle, body, image, tileImage }) {
      this.id = id;
      this.title = title;
      this.tabTitle = tabTitle;
      this.body = body;
      this.image = image;
      this.tileImage = tileImage;
    }
  }

export default noticia;